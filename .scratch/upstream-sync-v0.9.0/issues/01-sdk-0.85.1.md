# Issue 01: sdk-0.85.1

Type: task
Status: resolved
Blocked by: —

Upgrade @earendil-works/pi-* 0.84.3 → 0.85.1 per upstream 06ed14f. Root package.json pin + npm install; port rpc-manager.ts 2-line hunk + rpc-manager-extension-ui/widgets test updates. Check for breaking changes affecting electron main-process SDK use. Gate: typecheck + npm test.

## Answer

Resolved in commit f97debd. Upstream 06ed14f ported: pins bumped, PlainTextTheme
muted/text keys, widgets test assertions. Note: git apply --3way is atomic — a
missing file in the patch set fails the whole apply despite per-file "cleanly"
messages; apply per-file when a commit touches files we do not carry yet.
Extension-ui test hunk deferred to issue 03 (file created by 7c5f4e6).
Gate: typecheck clean, unit 751/753 (1 known Windows PATH baseline fail),
E2E 7/7. New SDK surfaces noticed: AgentSession.abortCompaction /
abortBranchSummary (test fakes now require them).
